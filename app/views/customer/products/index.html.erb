<% content_for :title, "Our Products" %>

<div class="max-w-6xl mx-auto">
  <h1 class="text-xl md:text-2xl font-bold text-gray-800 mb-4 md:mb-6">Order Water Bottles 🧴</h1>

  <% if @products.any? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6">
      <% @products.each do |product| %>
        <div class="bg-white p-4 md:p-6 rounded-xl shadow hover:shadow-lg transition">
          <div class="h-32 md:h-40 bg-gray-100 rounded-lg mb-3 md:mb-4 flex items-center justify-center">
            <% if product.image.present? %>
              <img src="<%= product.image %>" class="h-full object-contain p-2" alt="<%= product.name %>">
            <% else %>
              <img src="<%= image_path 'bottle.png' %>" class="h-full object-contain p-2" alt="<%= product.name %>">
            <% end %>
          </div>

          <h3 class="font-semibold text-base md:text-lg mb-1"><%= product.name %></h3>
          <p class="text-gray-500 text-xs md:text-sm mb-2 line-clamp-2"><%= product.description %></p>
          <p class="text-xs md:text-sm text-gray-600 mb-2">Size: <%= product.size %></p>

          <div class="flex justify-between items-center">
            <span class="font-bold text-lg md:text-xl">₹<%= product.price %></span>
            <span class="text-xs text-gray-500">Stock: <%= product.stock_quantity %></span>
          </div>
        </div>
      <% end %>
    </div>

    <div class="mt-6 md:mt-8 text-center">
      <%= link_to "Place Order", new_customer_order_path,
        class: "bg-[#0d2c63] text-white px-6 md:px-8 py-2 md:py-3 rounded-lg hover:scale-105 transition inline-block text-base md:text-lg font-medium" %>
    </div>
  <% else %>
    <div class="text-center py-12 bg-white rounded-xl shadow">
      <p class="text-gray-500 text-base md:text-lg">No products available right now.</p>
    </div>
  <% end %>
</div>
